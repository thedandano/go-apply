# Binaries
/go-apply
bin/
dist/

# Test artifacts
*.test
coverage.out

# Config and data dirs (user-local)
*.db

# OS
.DS_Store

# Git worktrees (local development isolation)
.worktrees/
.claude/worktrees/

# Local spec-kit harness (per-user, not shared) — skill defs, harness
# scripts/templates/extensions, and generated feature specs live off-repo
.claude/skills/speckit-*/
.specify/
specs/

# Local retros (never committed)
docs/retros/
openspec/
