# Binaries
/go-apply
bin/
dist/

# Test artifacts
*.test
coverage.out

# Config and data dirs (user-local)
*.db

# OS
.DS_Store

# Git worktrees (local development isolation)
.worktrees/
.claude/worktrees/

# Local retros (never committed)
docs/retros/
openspec/

# Claude local tooling (skills, commands — not project source)
.claude/commands/
.claude/skills/

# Speckit tooling (local workflow — not project source)
.specify/
